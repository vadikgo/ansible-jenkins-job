---
- hosts: localhost
  connection: local
  tasks:
    - jenkins_build:
        params:
          p1: 111
        name: test
        url: http://localhost:8080
        user: admin
        password: admin
